import 'package:flutter_elearning_app/core/app_export.dart';

class PopularInstructorController extends GetxController {
  // For now keeping it simple, can be expanded later
  RxList populerInstructor = [].obs;
}
